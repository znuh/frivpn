language: c

os:
  - linux

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install liblua5.2-dev libssl-dev liblzo2-dev

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make
